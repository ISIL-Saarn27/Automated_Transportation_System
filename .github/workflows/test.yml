name: Runner Diagnostics

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Show User
        shell: cmd
        run: |
          whoami

      - name: Show PATH
        shell: cmd
        run: |
          echo %PATH%